# RoadLens Planner 开发路线图

## 产品定位

RoadLens Planner 是面向个人自驾摄影创作者的地图化路线规划与拍摄资料系统。核心闭环是：

```text
选择城市、时间和拍摄模式
→ 获得真实地点组成的路线
→ 查看最佳时段、天气和设备参数
→ 创建拍摄计划
→ 拍摄后沉淀验证结果与视频
```

它不是旅游攻略，也不以地点数量作为首要成功指标。

## 当前里程碑：契约与纵向样例

完成标准：

- 版本化 JSON Schema 是唯一数据契约。
- 地点坐标明确使用 GCJ-02。
- 至少 3 条小规模路线通过结构和引用校验。
- 来源检查与实地检查可区分。
- CLI 能按城市、模式和时长读取数据并给出路线、途经点、设备和建议。

## MVP：地图拍摄闭环

按纵向切片开发：

1. 地图展示已校验地点与路线。
2. 按城市、白天/夜间、时长筛选。
3. 路线详情展示有序拍摄点、到达方式、拍摄建议和来源状态。
4. 将路线保存为拍摄计划。
5. 拍摄后把计划和路线更新为 `captured`，并允许升级为 `field-checked`。

当前进度：地图式探索界面、路线筛选、路线详情、AMap.Driving 实时驾车规划和设备本地持久化拍摄计划已完成首版；下一步完善地点库与实地核验回填。

首批范围限制在广东少量人工核验路线。地图闭环稳定后再扩充数据，不预设 50 或 100 条数量目标。

## 后续阶段

### ASMR 与自然声音

在 Location 契约增加经过版本控制的声音环境字段，重点记录噪声、人流、天气与收音建议。

### 个人资产 Dashboard

统计路线、实地核验地点、拍摄计划和已发布视频。Dashboard 只消费服务层数据。

### 轨迹与导航

支持 GPX 导入、坐标系转换和路线 polyline。实时导航交给地图服务，不把静态样例包装成实时道路信息。

### 数据存储演进

数据量和多设备同步真正产生需求后，再从 JSON 迁移到 SQLite 或服务端数据库。迁移不得改变领域语义。

### AI 推荐

输入当前位置、可用时间、天气、设备和拍摄目标，输出可解释的推荐；只允许引用通过数据校验的实体，并显示验证状态。

## 工程基线

前端目标技术栈保持为 React、TypeScript、Vite、Tailwind CSS、shadcn/ui 和 Zustand，高德地图作为第一阶段地图服务。

每次合并至少通过：

```bash
npm test
```

数据字段、枚举和状态不得在本文件定义，统一参见 `schema.md` 与 `schemas/roadlens-catalog.schema.json`。
