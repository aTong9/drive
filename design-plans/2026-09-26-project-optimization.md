# 项目整体优化开发 · 2026-09-26

延续现有自然手账视觉和11页职责，基于当前代码复核，不以此前完成记录代替本轮验证。不增加无明确用途的框架或依赖。

## 执行范围与完成依据

- 数据可靠性：项目/核验导入、持久化与迁移；损坏数据应明确拒绝且不覆盖当前项目。用实际入口回归测试验证。
- 关键交互：地点与路线、计划、项目、后期与发布资料的跨页衔接；复核侧栏键盘操作、状态与返回位置。桌面/手机浏览器验证。
- 视觉与可访问性：维护工具卡、表单、详情层、双主题；以渲染页面的焦点、布局和阅读为依据。
- 性能与交付：现有懒加载、包体检查、错误回退；运行完整测试与构建，针对实测瓶颈修改。

## 当前证据

- 起始git工作区干净。
- 新增回归测试复现mediaBatches=false错误通过validateVideoProject，预期false实际true。校验已改为仅允许字段缺省或有效数组，兼容旧项目缺省值；拒绝false、0、空字符串与null。
- 其余范围仍待本轮检查，不能以这一处修复宣称整体优化完成。

- 核验JSON导入新增文本字段与真实日期校验：原先停车说明为对象仍被接受，回归测试以Missing expected rejection复现；修复后拒绝对象/数组/数字说明、错误更新时间和不存在的访问日期。文件内全部记录验证成功后才返回给页面写入，保持失败不部分导入。
- 项目与核验相关17项服务测试、TypeScript通过。待继续：侧栏键盘/焦点、跨页浏览器与最终性能构建验收。

### 详情键盘与响应式回归

- 修复前实测：Enter打开青海湖地点，焦点仍在卡片，Escape后详情仍可见。现在打开后聚焦关闭按钮，Escape关闭并返回原卡片；焦点移动使用preventScroll，点击其他区域不强制抢回焦点。面板使用原生局部键盘监听，不干扰通过portal打开的照片弹层。
- 本轮浏览器：桌面地点/路线容器均为x0/y68/宽1164；手机390px下两标签y均393.039，文档宽390，无横向溢出；手机详情焦点与Escape返回均通过。已恢复默认视口。
- 完整npm test通过：266项服务测试、1项脚本测试，以及数据校验、类型检查、计划示例；npm run build与包体预算通过。
- 整体目标仍有跨页业务链、双主题和实际导入入口验收待完成，本轮不标记整体完成。

可运行的浏览器键盘回归（Codex CUA，qa为当前本地预览的Playwright tab，qaTab为同一原生tab；先进入地点库）：

```js
await qa.playwright.getByRole('button', { name: '青海湖二郎剑景区缩略图 湖泊 · 海南州 青海湖二郎剑景区 停车后步行 来源', exact: true }).press('Enter');
if (!(await qa.playwright.evaluate(() => document.activeElement?.getAttribute('aria-label') === '关闭地点详情'))) throw new Error('打开后焦点未进入详情');
await qaTab.pressKey(null, 'Escape');
if (!(await qa.playwright.evaluate(() => document.querySelector('.location-detail')?.classList.contains('is-hidden') && document.activeElement?.textContent?.includes('青海湖二郎剑景区')))) throw new Error('关闭或焦点返回失败');
```

### 跨页与旧数据清理

- 上一目标回合属于有效进展：补齐详情焦点并完成桌面/手机验证，本回合继续业务链验收。
- 浏览器实测：搜索青海湖→打开地点→关联路线，实际显示青海湖二郎剑至茶卡盐湖高原路线；返回地点库，搜索词和所选详情均保留。
- 深色主题地点库维护卡、筛选栏、搜索与卡片渲染正常；已恢复原浅色主题。
- 实际导入入口选择包含对象parkingNote的JSON，显示“核验字段不完整或日期无效”；核验仍为空、导出仍禁用，未写入损坏数据。
- 计划→继续视频项目→进入后期，三页标题均为太原滨河东路汾河城市夜驾路线，后期当前项目显示相同途经点。
- 发现旧持久化项目交付标签仍含退役品牌；回归测试先失败，随后normalizeVideoProject清理旧标签，persist版本11触发已有数据迁移，导入复用同一入口。保留项目条目id、完成状态与备注，重复归一化结果不变。
- 刷新后浏览器实证：项目正文不含旧品牌、交付标签为“Vision 与 Ambience 画面长度一致”，进度仍5/56；3条计划与4个项目均仍可访问。
- 待完成：项目损坏文件实际入口、发布资料衔接、最终整体完成审计。

## 本轮完成审计 · 2026-09-26

上一回合为有效进展：实际迁移旧项目品牌标签并验证数据保留。本节覆盖前文随执行产生的待办状态。

| 本轮要求 | 当前实现与本轮证据 | 结论 |
| --- | --- | --- |
| 数据可靠性：项目/核验导入与迁移 | 两种导入均在完整校验后写入；falsey非数组素材、错误核验字段/日期有失败后修复回归。浏览器分别导入损坏文件，显示明确错误，原4个项目和空核验状态保持。版本11迁移后计划、项目进度保留，旧标签清除。 | 完成 |
| 关键交互与跨页返回 | 地点→关联路线→返回保留青海湖查询与详情；计划→视频项目→后期标题和途经点匹配；项目→准备发布资料正确选中同一项目并呈现其标题、路线和设备。桌面及手机地点/路线切换位置不变；详情打开、Esc与焦点返回通过。 | 完成 |
| 维护卡、详情与双主题 | 当前维护区为折叠工具卡，实际入口可用；浅色手机390px截图无横向溢出，深色桌面维护卡/筛选/搜索/结果层级清晰；恢复原主题和视口。 | 完成 |
| 性能与交付 | App各工作台保持lazy/Suspense；高德天气与路线失败显示文本提示和路线锚点，导入失败保持页面可用。最新npm test退出0（267服务+1脚本），包含数据与类型检查；npm run build退出0，PWA生成成功，最大JS270.6KiB、212个目录分片通过预算；git diff --check通过。 | 完成 |
| 既有全站设计与全局旧品牌移除 | 既有11页共享journal主题及第19日方案职责继续保留；本轮额外修复已保存项目的旧品牌残留。源代码/数据/公开资源检索仅剩有意处理旧标签的迁移匹配式及测试输入。 | 完成 |

本轮修改不新增依赖、不改变项目数据结构或完成状态语义。整体优化开发的本轮执行范围已完成；不包含提交、推送或上线。高德实时天气/驾车成功响应仍取决于外部服务与域名授权，已核对失败提示，不宣称外部服务已恢复。

最新日志：/tmp/roadlens-sep26-migration-test.log、/tmp/roadlens-sep26-migration-build.log。

## 继续优化：发布资料一致性

本轮从实际发布页发现并修复三处资料不一致：固定00:45占位覆盖项目章节、候选音乐被描述为已授权、Ambience简介混入Vision音乐署名。

- 中英文三种模板直接使用项目章节，每份简介仅出现一次；空章节保留明确待填提示，不编造时间码。保留原始章节文字，不自动翻译用户内容。
- Vision仅在非空音乐台账全部处于licensed/clearlisted时使用已授权措辞；无曲目或任一候选曲目均提示发布前核对许可。通用项目简介不再无条件宣称授权。
- Ambience生成资料仅引用该频道，不带入Vision曲目署名；原项目台账不作修改。
- 回归先以英文简介未包含保存章节（0，预期1）失败，再修复；覆盖三模板、候选、已许可、clearlisted、混合许可、空音乐、双频道。
- 浏览器实际Vision页面：保存章节存在、00:45不存在、许可待核对提示存在、已授权声明不存在；切到Ambience后章节保留、无音乐承诺保留、音乐署名不存在。
- npm test退出0：268服务+1脚本，数据与类型校验通过；npm run build和包体预算通过，git diff --check通过。日志/tmp/roadlens-publish-final-test.log和/tmp/roadlens-publish-final-build.log。
- 本轮继续优化完成，未提交、推送或发布。
